With only having access to the step() function Policy and Value iteration can no longer be applied in their original form.
While not using those two iteration functions we are now in a model free RL setting,
where we would need to approximate the transition and reward instead of exactly calculating them.